@model LagunaShoreResort2.Models.RolSalesMember

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>RolSalesMember</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.rolSalesMemberID)

        <div class="form-group">
            @Html.LabelFor(model => model.rolID, "rolID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("rolID", String.Empty)
                @Html.ValidationMessageFor(model => model.rolID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.salesMemeberID, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.salesMemeberID)
                @Html.ValidationMessageFor(model => model.salesMemeberID)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
